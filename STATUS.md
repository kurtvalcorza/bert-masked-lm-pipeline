# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package with its adaptation contract (`evaluate`, `unigram_baseline`, `adapt`, `save_artifact` / `from_artifact`; the pinned SciTLDR-A abstract corpus and BYOD loaders in `samples.py`; seeded masking, masked perplexity, top-k accuracy and the unigram floor in `metrics.py`), offline unit tests (34, no weights required, plus 5 notebook-parity tests and 2 model-backed tests that run when the snapshot is staged), a local CPU execution of the committed notebook blob in the Windows venv (frozen test masked perplexity 15.09 → adapted 12.59 against a unigram floor of 1,174.6; recorded in `docs/release-verification.md`), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial notebook `tutorials/bert_masked_lm_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) with its static validator, generator parity checks and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. No hosted run of any revision of this notebook has been recorded. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
