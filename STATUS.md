# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package, offline unit tests (12), one CPU smoke run on the verified snapshot, the MODEL_CARD_SPEC 1.1 `MODEL_CARD.md`, and the `TASK-INFERENCE` tutorial notebook `tutorials/bert_masked_lm_colab.ipynb` with its static validator and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
