# Release status

Current status: **Candidate / source-complete**. The pipeline package, offline unit tests (12), one CPU smoke run on the verified snapshot, and the MODEL_CARD_SPEC 1.1 `MODEL_CARD.md` exist (card pass only). No tutorial notebook ships yet; the repository is not release-grade until a `NOTEBOOK_SPEC` 1.0 tutorial exists and clean-runtime execution evidence for the exact candidate commit is recorded.
